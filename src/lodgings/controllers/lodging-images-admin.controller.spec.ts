describe('LodgingImagesAdminController', () => {
  it.todo(
    'debe delegar createUploadUrl con ownerId y role del usuario autenticado',
  );
  it.todo(
    'debe delegar confirmUpload con ownerId y role del usuario autenticado',
  );
  it.todo('debe delegar setDefault con ownerId y role del usuario autenticado');
  it.todo('debe delegar delete con ownerId y role del usuario autenticado');
});
